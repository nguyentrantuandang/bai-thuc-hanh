from itertools import islice

def read_from_head(fname, nlines):
    with open(fname) as f:
        for line in islice(f, nlines):
            print(line.strip())

# Ví dụ sử dụng:
# read_from_head('test.txt', 3)
