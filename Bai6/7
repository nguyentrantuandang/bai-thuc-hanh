class Circle:
    def __init__(self, r):
        self.radius = r
        self.pi = 3.14

    def area(self):
        return self.pi * self.radius * self.radius

    def perimeter(self):
        return 2 * self.pi * self.radius

# Ví dụ sử dụng:
# circle1 = Circle(7)
# print(circle1.area())
# print(circle1.perimeter())
